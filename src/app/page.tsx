import { FC } from 'react';

interface pageProps {}

const page: FC<pageProps> = () => {
  return (
    <main className="min-h-screen flex justify-center items-center"></main>
  );
};

export default page;
